<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Today's Outfit - What Should I Wear?</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="weather-container">
      <div class="weather-header">
        <h1>Today's Outfit Ideas for <%= city %></h1>
        <p class="date"><%= today %></p>

        <div class="weather-summary">
          <div class="weather-icon">
            <img src="<%= weather.icon %>" alt="<%= weather.condition %>" />
          </div>

          <div class="weather-text">
            <div class="weather-info">
              <h2><%= weather.condition %>: <%= weather.description %></h2>
            </div>
      
            <div class="weather-temp">
              <h2>
                <%= weather.temp %>°F: feels like <%= weather.feels_like %>°F
              </h2>
            </div>
          </div>
        </div>
      </div>

      <div class="weather-details">
        <div class="detail">
          <span class="label">💧 Humidity</span>
          <span class="value"><%= weather.humidity %>%</span>
        </div>
        <div class="detail">
          <span class="label">💨 Wind</span>
          <span class="value"><%= weather.wind_speed %> mph</span>
        </div>
        <div class="detail">
          <span class="label">📍 </span>
          <span class="value"><%= weather.city %>, <%= weather.country %></span>
        </div>
      </div>
    </div>

    <!-- Outfit Advice -->
    <div class="outfit-container"
      <h2>👕 Recommended Outfit</h2>
      <p>Based on today's weather conditions, consider the following:</p>

      <div class="outfit-list">
        <% outfit.forEach((item, index) => { %>
        <div class="outfit-item">
          <div class="item-number"><%= index + 1 %></div>
          <div class="item-text"><%= item %></div>
        </div>
        <% }) %>
      </div>

      <div class="home-btn">
        <a href="/" class="btn-primary">← Check Another City</a>
      </div>
    </div>
  </body>
</html>
